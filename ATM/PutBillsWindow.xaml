<Window x:Class="ATM.PutBillsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStyle="None"
        Title="PinCodeWindow" Width="1920" Height="1080">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Click="CancelButton_Click" Grid.Row="0" Grid.Column="0" Style="{StaticResource NegativeButton}"
                    Margin="100 15 100 15">
                Отмена
            </Button>

            <Button Click="ContinueButton_Click" Grid.Row="0" Grid.Column="1" Style="{StaticResource PositiveButton}"
                    Margin="100 15 100 15">
                Продолжить
            </Button>
        </Grid>
        <TextBlock FontSize="80" TextAlignment="Center" VerticalAlignment="Center" Margin="100 50">
            Положите купюры <LineBreak />в купюроприемник
        </TextBlock>

    </DockPanel>
</Window>